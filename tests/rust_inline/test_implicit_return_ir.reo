
@repr(C)
struct Timespec {
    tv_sec: i64,
    tv_nsec: i64,
}
@repr(C)
struct MyStat {
    st_size: i64,
    st_atim: Timespec,
}
fn main() {
    let ts = Timespec { tv_sec: 1000, tv_nsec: 0 }
    let s = MyStat { st_size: 42, st_atim: ts }
    let sec = s.st_atim.tv_sec
    println(sec)
}
