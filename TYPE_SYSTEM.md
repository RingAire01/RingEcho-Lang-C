# 精确 C 类型与系统级布局

## C 后端替换状态

C 后端的旧数组指针表示、统一宽槽数组、整数化引用和整数化函数值已移除。
`c_storage.c` 统一登记具体类型、按依赖顺序生成声明；`c_sequence.c` 负责
序列访问与可写位置。无法确定的类型报告错误，不再退回 `int64_t`。

| 源类型 | 当前 C 表示与语义 |
|--------|------------------|
| 固定数组 `[T; N]` | 含 `T data[N]` 的值类型；复制、参数传递、返回值包含元素本身，无隐式堆分配 |
| 动态切片 `[T]` | `T *data` + `size_t len`，元素类型和步长精确 |
| `&T` / `&mut T` | 指向真实 T 的只读/可写 C 指针；保留逐层 const |
| `*const T` / `*mut T` / `*T` | 保留 T 的裸指针；`*T` 默认为可写 |
| `ptr` | 明确不透明的 `void*`，不能在没有目标类型时解引用 |
| `fn(A...)->R` | 真实、带完整签名的 C 函数指针；不经过整数，不添加伪造的环境参数 |
| `Vec<T>` / `vec` / `svec` | 指向专门化容器对象的真实指针；对象含 `T *data`、长度和容量；后两者分别为 i64/str 容器 |
| 元组 | 带实际成员类型的值结构体，字段使用 `f0`、`f1` 等名称 |
| `unit` | GNU C 零大小结构体；函数的 unit 返回类型使用 void |
| 命名结构体、组件、用户枚举 | 真实字段类型与对齐；枚举保存完整有效载荷，多参数有效载荷为字段组 |
| checked `Result<T, ConversionError>` | 保留原有 tag/value/error/index 诊断接口；数组 value 现在内联存储 |

非泛型核心 Option/Result 的载荷仍按既有 i64 协议处理；它们不是任意类型
的整数容器。checked Result 的诊断结构与普通枚举布局需区别使用。

旧数组运行时已删除，checked 数组转换直接操作真实元素并返回内联结果。
函数值、目录句柄、GC 根节点和容器传递均不再通过整数伪装地址。
目录打开失败现在返回空指针，使用 `handle == 0` 判断。

### 兼容性与生命周期

- 固定数组及包含它们的结构体 ABI 已改变，必须重建所有相关目标文件和库。
  固定数组复制不再共享元素存储。
- Vec 保留共享容器语义：复制值会复制类型化句柄，指向同一个容器对象。
  变量本身为一个指针；“指针 + 长度 + 容量”是被分配对象的布局。
  `free(vector)` 销毁容器，别名随后不得使用或再次释放。
- 动态重复表达式生成的切片分配真实 T 元素，调用者通过 `free(slice)` 释放。
  切片复制共享数据，释放仅一次；这些缓冲区不依赖自动 GC 猜测栈上根节点。
- 字符串容器保存自己的字符串副本；修改/销毁容器后，先前取得的借用字符串
  不应继续使用。`svec_get` 的越界空串行为保留。
- 编译期拒绝只读位置写入、明显的局部借用返回及局部借用向间接存储转移。
  这不是完整的 Rust 式借用检查器；裸指针、FFI 和手动释放仍要求调用者遵守
  有效性、别名和生命周期约束。
- 函数指针间接调用只求值一次并检查空指针。Lambda 仍不支持捕获环境。
- 泛型函数和 Lambda 分批收集依赖后生成原型，保留具体签名，不修改原始 AST。

## 布局服务

`include/analysis/layout.h` / `src/analysis/layout.c` 提供编译任务级
`Re0LayoutManager`。节点、字段和名称由 manager 统一拥有并释放；没有全局
可变布局状态。支持基础类型、引用、函数指针、数组、切片、容器、元组、
结构体、枚举、别名以及具体泛型实例的大小、对齐与成员偏移计算。

未解决的 unknown/类型参数不能生成运行时布局。按值递归拒绝；通过引用、
裸指针或容器句柄递归不展开无限对象。Vec 的值大小为目标指针大小，
`allocation_size` 描述其三字段容器对象的大小。

SysV AMD64 的 INTEGER/SSE/MEMORY 分类可用于 ABI 查询，但分类器本身不等于
原生后端已实现所有聚合参数传递和隐藏返回指针。

## 原生后端边界

Native 仍为 [NATIVE.md](NATIVE.md) 中的实验性子集：8–64 位整数、
isize/usize、char、bool、f32/f64 和 unit 返回值。其 i128、聚合值、引用、
字符串、容器和泛型完整代码生成仍需后续扩展。C 后端此次替换完成不代表
“原生后端全部类型支持”完成，也没有通过回退调用 C 编译器伪装原生支持。

## 验证与维护

```bash
make CONFIG=Release build
make CONFIG=Release test test-match test-conversion test-native
make CONFIG=Release test-layout test-layout-faults test-native-unit test-c-storage-faults
REO_TEST_COMPILER="$PWD/target/Release/rev" REO_TEST_SANITIZE_GENERATED=1 \
  python3 -m unittest discover -s tests/native -p 'test_c_storage.py'
```

回归检查覆盖源代码和生成 C 中的旧表示残留，固定数组尺寸/复制/返回、嵌套
数组、精确元素步长、可写引用、裸指针、函数值、泛型/Lambda 依赖、递归
间接类型、unit/元组和动态容器。生成 C 使用关键指针与函数签名错误检查，
这些检查也已加入正常 C 构建命令。另有编译器及生成程序的 ASan/UBSan 检查，
类型布局/ELF/C 类型注册表的分配失败注入和文件短写测试。

注册表、递归深度、字段和序列长度有明确上限；错误不会用整数占位继续生成
可执行代码。C 后端状态按线程隔离，同一编译上下文须在单一线程内使用。
审查范围是本次表示替换及受影响边界，不把通过回归测试等同于任意用户程序
在使用裸指针或 FFI 时都具有内存安全保证。
